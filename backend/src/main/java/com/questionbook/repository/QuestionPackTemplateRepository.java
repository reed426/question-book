package com.questionbook.repository;

import com.questionbook.entity.QuestionPackTemplate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QuestionPackTemplateRepository extends JpaRepository<QuestionPackTemplate, Long> {}
